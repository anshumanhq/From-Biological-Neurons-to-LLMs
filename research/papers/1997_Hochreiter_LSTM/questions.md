# Open Questions: LSTM (1997)

1. How does the choice of gate activation functions affect performance?
2. Can LSTM be further simplified without losing long-term memory capability?
3. What is the optimal ratio of hidden size to input size for a given task?
4. How does LSTM compare to GRU on various sequence lengths?
5. Are there tasks where LSTM still outperforms Transformers?