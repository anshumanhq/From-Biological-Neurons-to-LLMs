# Teacher Forcing

A training technique for recurrent networks where the target output is used as the next input (or context) during training, rather than the network's own prediction. This stabilises learning but can cause exposure bias.
