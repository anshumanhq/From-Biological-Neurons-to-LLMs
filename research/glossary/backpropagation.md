# Backpropagation

A supervised learning algorithm for training neural networks. It computes the gradient of the loss with respect to all weights using the chain rule, propagating errors backward from the output layer to the input layer.

Key idea: **Reverse-mode automatic differentiation** applied to a computational graph.
