# Backpropagation Through Time (BPTT)

An extension of backpropagation for recurrent neural networks. The network is unfolded over time, and gradients are computed across all time steps. It suffers from vanishing gradients when sequences are long.
