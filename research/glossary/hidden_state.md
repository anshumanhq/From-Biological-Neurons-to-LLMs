# Hidden State

The internal representation of a recurrent network at a given time step. It summarises information from past inputs and influences future outputs. In Elman networks, the hidden state is fed back as context.
