# Attention

A mechanism that allows a network to focus on relevant parts of the input when producing an output. In Transformers, self-attention computes relationships between all positions in a sequence, enabling parallel processing and long-range dependencies.
